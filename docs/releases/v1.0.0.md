# v1.0.0 — Governed Agentic Quality Engineering Platform

This first stable portfolio release demonstrates how specialized AI agents can assist Quality Engineering while deterministic controls and accountable people retain authority.

## Included evidence

- nine specialized QE agents with explicit inputs, outputs and restrictions
- LangGraph checkpoints, pause/resume, retry bounds and loop guards
- deterministic risk, traceability, coverage and regression-selection controls
- policy-validated Playwright generation with hash-bound human approval
- real Playwright JSON execution, evidence-aware triage and UNKNOWN fallback
- RBAC, tenant-scoped persistence, audit events, telemetry and offline evaluation
- a 66-second recruiter walkthrough and five-minute Docker demonstration

## Verification

```bash
cp .env.example .env
# set JWT_SECRET and DEMO_PASSWORD
docker compose up --build -d
docker compose exec api python scripts/seed_demo.py
curl http://localhost:8080/health
```

## Scope and limitations

This is a synthetic portfolio reference, not an autonomous production release authority. AI recommendations remain bounded by deterministic policy, human approval and organization-specific calibration.
